import AiImageGenerator from "./Components/Ai-Image-Generator/AiImageGenerator";

function App() {
  return (
    <div>
      <AiImageGenerator />
      {/* <input type="text" className="ai-input" />
    <button onClick={()=>{imageGenerator()}}>Click Me</button>
    <img src={image_url} alt="" />
    <button><a href={image_url} download={image_url}>Download</a></button> */}
    </div>
  );
}

export default App;
